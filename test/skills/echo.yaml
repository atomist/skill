apiVersion: 1
skill:
  namespace: atomist
  name: echo-skill
  artifacts:
    docker:
      - name: echo
        image: ubuntu:latest
        command:
          - /bin/sh
        args:
          - -c
          - "cat $ATOMIST_PAYLOAD"
