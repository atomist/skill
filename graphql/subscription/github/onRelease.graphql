subscription onRelease {
  Release {
    id
    name
    tag {
      name
      description
      timestamp
      release {
        name
        id
        timestamp
      }
      commit {
        sha
        message
        repo {
          ...repoFields
        }
      }
    }
  }
}
